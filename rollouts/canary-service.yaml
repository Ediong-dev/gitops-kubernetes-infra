apiVersion: v1
kind: Service
metadata:
  name: portfolio-web-app-canary
  namespace: portfolio-production
spec:
  type: ClusterIP
  selector:
    app: portfolio-web   # selector will be updated by Rollout
  ports:
  - port: 80
    targetPort: 80