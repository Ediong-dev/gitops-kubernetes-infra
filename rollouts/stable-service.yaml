apiVersion: v1
kind: Service
metadata:
  name: portfolio-web-app-stable
  namespace: portfolio-production
spec:
  type: ClusterIP
  selector:
    app: portfolio-web   # initially points to all pods; Rollout will manage
  ports:
  - port: 80
    targetPort: 80